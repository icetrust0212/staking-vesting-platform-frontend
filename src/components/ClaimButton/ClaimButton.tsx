import RoundButton from "../RoundButton/RoundButton";

const ClaimButton = () => {
    const claim = () => {

    }

    return (
        <RoundButton onClick={claim} disable={true}>Claim</RoundButton>
    )
}

export default ClaimButton;